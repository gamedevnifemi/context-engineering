#!/bin/sh
# Repository content checks for the commit message. See docs/privacy.md.
root="$(git rev-parse --show-toplevel)"
if command -v python >/dev/null 2>&1; then py=python; else py=python3; fi
exec "$py" "$root/scripts/privacy_check.py" --message "$1"
